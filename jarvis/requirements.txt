# Core — enough to run the assistant, tools, planner, and UI
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
websockets>=12
httpx>=0.27
pyyaml>=6
pydantic>=2
psutil>=5.9
pillow>=10
platformdirs>=4

# Optional native computer-control extras (install if you want global hotkeys / extra input)
# pynput>=1.7
# opencv-python>=4.8

# Optional local speech (pick what fits your hardware)
# sounddevice>=0.4
# faster-whisper>=1.0
# vosk>=0.3
# pyttsx3>=2.90
# openwakeword>=0.5
